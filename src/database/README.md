# Database

Use esta pasta para scripts SQL, migrations, seeds ou documentacao de tabelas quando o projeto deixar de depender apenas da configuracao externa do Supabase.

Estado atual:

- O projeto usa Supabase como banco remoto.
- As credenciais ficam em `.env` ou `.env.local`.
- O client fica em `src/services/supabase/client.js`.
